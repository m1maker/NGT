void main(){
tts_voice v;
size_t size;
string pcm=v.speak_to_memory("Test", size);
sound s;
s.load_pcm(pcm, size, 1, 16000);
show_window("");
while(!quit_requested){
wait_event();
if(key_pressed(KEY_SPACE))s.play();
}
}